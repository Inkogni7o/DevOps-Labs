# AGENTS.md

---

## Назначение

Этот файл является основным операционным руководством для AI-агентов, работающих в репозитории.

Он должен давать агенту достаточно контекста, чтобы:

- понимать, что делает проект;
- следовать правильной архитектуре и принятым соглашениям;
- класть код в правильные директории;
- запускать нужные команды для setup, проверки и тестирования;
- избегать небезопасных и некачественных изменений;
- понимать, когда нужно остановиться и спросить человека.

Если этот файл расплывчатый, устаревший или противоречивый, качество работы агента будет заметно хуже. Делайте его конкретным.

---

## Краткая Сводка По Проекту

- Название проекта: `kubernetes-lab1`
- Тип проекта: `web-app + api`
- Однострочное описание: `Веб интернет магазина + API`
- Бизнес- или доменный контекст: `интернет магазин`
- Стадия жизненного цикла: `mvp для дальнейшего использования в лабораторных работах по Kubernetes`
- Владельцы / ответственная команда: `@thunboo`

---

## Принципы Работы Агента

Если пользователь явно не попросил иначе, агент должен:

- предпочитать минимальное безопасное изменение, которое решает задачу;
- сохранять существующую архитектуру и naming conventions;
- обновлять тесты при изменении поведения;
- обновлять docs, config и examples, если они устаревают из-за изменений;
- проверять результат перед завершением работы;
- не делать спекулятивных рефакторингов;
- спрашивать перед разрушительными, необратимыми, дорогими или влияющими на production действиями.

### На Что Агент Должен Оптимизировать Работу

1. Correctness
2. Maintainability
3. Speed

### Чего Агент Не Должен Делать По Умолчанию

- Переписывать архитектуру без запроса.
- Добавлять новую зависимость, если задачу можно решить уже существующими зависимостями проекта.
- Редактировать сгенерированные файлы вручную, если в проекте принят соответствующий workflow.
- Игнорировать падающие проверки, связанные с изменёнными файлами или поведением.
- Совершать действия наугад в security-sensitive, billing-sensitive или compliance-sensitive областях.

---

## Источники Правды

Перед любыми нетривиальными изменениями сверяйтесь со следующими материалами:

| Источник | Путь / URL | Когда использовать |
| --- | --- | --- |
| MVP требования | `MVP-requirements.ru.md` | `Когда необходимо понять, что стоит, а что НЕ стоит добавлять в проект, если пользовательский комментариев недостаточно (ВСЕ РАВНО ПЕРЕСПРАШИВАТЬ ПОЛЬЗОВАТЕЛЯ, если что-то явно не отмечено в указанном файле)` |

Если документация и код противоречат друг другу, приоритет у `ask_human`, а само расхождение нужно упомянуть в финальном summary.

---

## Технологический Стек

Не пишите “latest”. Указывайте точные версии или поддерживаемые диапазоны.

### Основной Стек

- Язык(и): `JavaScript, Python3`
- Runtime(s): `python3.14, etc`
- Framework(s): `React, Next.js, FastAPI`
- База(ы) данных: `PostgreSQL 17`
- Messaging / queueing: `Python Celery`
- Кэш / хранилище: `Redis`
- Хостинг / инфраструктура: `on-prem`

+ Содержимое файла `TECH-STACK.md`

---

## Архитектура

- Архитектурный стиль: `modular monolith`
- Основные модули / bounded contexts: `users, orders, products, carts, payments, inventory, admin`

---

## Структура Репозитория

Ты работаешь только в папке `Kubernetes/Lab0/`

### Правила Размещения Файлов

```md
app/web/          # customer-facing frontend
app/admin/        # internal operations frontend
app/backend/      # backend code
app/tests/         # folder containing tests (not neccessary, we will do only his if User asks to do so)
docs/             # architecture notes, onboarding docs
```

---

## Подготовка Окружения

### Обязательные Инструменты

```md
- Код НЕ будет запускаться напрямую на машинке, поэтому только все в Dockerfile для запуска в docker и kubernetes
- Откуда загружать env-переменные: .env.local # Заполнишь тестовыми данными сам
```

## Стиль Кода И Naming

* Formatter: `Prettier` для frontend, `Ruff Formatter` для backend
* Linter: `ESLint` для frontend, `Ruff` для backend
* Политика типизации: `strict`
* Политика комментариев: комментарии используются только там, где код не объясняет намерение сам: сложная бизнес-логика, нетривиальные алгоритмы, важные ограничения, причины workaround-решений. Очевидные действия не комментируются.
* Политика импортов: `absolute`, `grouped`, `sorted`
* Подход к обработке ошибок: backend использует `exceptions` с централизованными exception handlers и единым форматом API-ошибок; frontend использует typed error wrappers для API-клиента и user-friendly error states в UI.
* Подход к логированию: `structured` logging на backend; на frontend — логирование только технически значимых ошибок без чувствительных данных.
* Подход к конфигурации: `typed env schema`; все переменные окружения описываются централизованно, валидируются при старте приложения и не читаются напрямую из `process.env` / `os.environ` внутри бизнес-логики.

### Naming Conventions, Которые Мы Предпочитаем

| Сущность                     | Предпочтительно                                                                                            | Избегать                                                        | Пример                                                      |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------- |
| Файлы                        | `kebab-case` для frontend-файлов, `snake_case` для Python-файлов                                           | смешивание стилей, `file1`, `newFile`, `utils2`                 | `product-card.tsx`, `order_service.py`                      |
| Директории                   | `kebab-case` для frontend, `snake_case` для backend-пакетов                                                | `Common`, `misc`, `stuff`, `new-folder`                         | `product-catalog/`, `order_processing/`                     |
| Классы / компоненты          | `PascalCase`                                                                                               | `camelCase`, абстрактные имена без смысла                       | `ProductCard`, `OrderService`                               |
| Функции / методы             | `camelCase` во frontend, `snake_case` в backend                                                            | слишком общие имена: `handle`, `process`, `doStuff`             | `fetchProducts`, `create_order`                             |
| Переменные                   | `camelCase` во frontend, `snake_case` в backend                                                            | однобуквенные имена вне коротких циклов, неочевидные сокращения | `selectedProductId`, `current_user`                         |
| Константы                    | `UPPER_SNAKE_CASE` для глобальных констант; `camelCase` / `snake_case` для локальных неизменяемых значений | магические числа и строки внутри логики                         | `MAX_RETRY_COUNT`, `DEFAULT_PAGE_SIZE`                      |
| Types / interfaces / schemas | `PascalCase`; backend-схемы с суффиксами `Request`, `Response`, `Create`, `Update`, `Read`                 | `IUser`, `TData`, `AnyObject`                                   | `ProductResponse`, `CreateOrderRequest`, `UserRead`         |
| Названия тестов              | описание поведения через `should...` / `test_...`                                                          | тесты по реализации вместо поведения                            | `shouldRenderProductPrice`, `test_create_order_returns_201` |
| Названия веток               | `<type>/<short-description>`                                                                               | `fix`, `changes`, `my-branch`, `test123`                        | `feature/product-catalog`, `fix/order-validation`           |

### Style Do / Don't

Делать:

* использовать названия, отражающие намерение;
* держать модули цельными и сфокусированными;
* следовать уже существующим паттернам рядом с изменяемым кодом;
* предпочитать явную бизнес-логику “умным” абстракциям;
* отделять API-слой, бизнес-логику, работу с БД и UI-состояние;
* валидировать входные данные на границах системы: API request, env config, формы;
* писать тесты на поведение, а не на внутреннюю реализацию.

Не делать:

* превращать `utils` в свалку несвязной логики;
* смешивать несколько naming styles в одной зоне кода;
* прятать важные side effects за расплывчатыми helper names;
* вводить широкие абстракции до появления второго реального use case;
* читать переменные окружения напрямую в разных частях приложения;
* логировать пароли, токены, cookie, персональные данные и содержимое секретов;
* использовать `any` / `dict` / `object` без необходимости и без последующей валидации.

---

## Границы Безопасности И Safety

Считайте этот раздел обязательным.

### Жёсткие Правила

- Никогда не коммитить secrets, private keys, access tokens или production credentials.
- Никогда не хардкодить secrets в source code, tests, fixtures или documentation.
- Редактировать чувствительные значения в логах и примерах.
- Валидировать и санитизировать недоверенный ввод на правильной границе.
- Использовать least privilege для database, cloud и service credentials.
- Быть особенно аккуратным в коде, связанном с auth, billing, PII, legal/compliance, infrastructure или permissions.

### Перед Этими Действиями Нужна Подтверждённая Проверка Человеком

- удаление данных или файлов;
- применение необратимых миграций;
- изменение auth или permission logic;
- изменение billing или payment flows;
- изменение deployment или production infrastructure;
- установка или замена крупных зависимостей;
- ротация secrets или изменение security configuration.

### Чувствительные Зоны

- Authentication / authorization
- Payments / billing
- Personal or regulated data
- Production configuration / infrastructure

---

### Definition Of Done

Изменение не считается завершённым, пока:

1. не прошли релевантные проверки;
2. не добавлены или не обновлены тесты там, где это нужно (пока что не используем этот пункт);
3. не обновлены docs/config/examples, если они затронуты;
4. размещение файлов и naming соответствуют этому документу;
5. assumptions, risks и follow-up work зафиксированы.

---

## Когда Агент Должен Остановиться И Спросить

Агент должен остановиться и спросить человека, если:

- требования неоднозначны и существует несколько валидных реализаций;
- изменение может нарушить совместимость API, данных или безопасность деплоя;
- документация и код существенно противоречат друг другу;
- тесты падают по причинам, не связанным с задачей, и причина неясна;
- для задачи нужны secrets, production access или product-policy decisions;
- самый безопасный путь зависит от tradeoff, который пользователь ещё не выбрал.

---

## Чеклист Поддержки Для Людей

- Обновляйте этот файл при изменении архитектуры, стека, команд или workflow.
- Следите, чтобы команды можно было выполнять в точности как они записаны.
- Перед rollout заменяйте расплывчатые плейсхолдеры реальными значениями.
- Добавляйте ссылки на лучшие примеры из репозитория для типовых задач.
- Разносите инструкции по вложенным `AGENTS.md`, когда один файл становится слишком широким по scope.
